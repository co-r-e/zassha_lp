# Required for accessing Gemini Files API
GEMINI_API_KEY=

# Optional: segment length (seconds) when ffmpeg is available
ZASSHA_SEGMENT_LEN=

# Optional: set when deploying to GitHub Pages project sites (e.g. "/zassha_lp")
NEXT_PUBLIC_BASE_PATH=
